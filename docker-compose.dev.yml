version: "3.8"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "3000:3000" # Map host 3000 → container 3000
    environment:
      - CHOKIDAR_USEPOLLING=true
